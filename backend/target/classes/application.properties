spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/job_tracker?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&useUnicode=true
spring.datasource.username=root
spring.datasource.password=Rafia529

# Ensure Hibernate uses UTC for JDBC timezone handling
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Prevent Spring Boot from auto-running schema.sql on startup
spring.sql.init.mode=never
